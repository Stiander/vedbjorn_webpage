
import classes from './Footer.module.css';

const Footer = () => {
    return (
        <div className={classes.footer}>
            Vedbjørn.no - v0.1 Open Alpha
        </div>
    );
};

export default Footer;
